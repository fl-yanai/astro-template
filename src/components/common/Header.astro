<script>
  const toggleButton = document.getElementById('header__hamburger-toggle')
  toggleButton?.addEventListener('click', () => {
    if (toggleButton.hasAttribute('open')) {
      toggleButton.removeAttribute('open')
    } else {
      toggleButton.setAttribute('open', '')
    }
  })
</script>

<header class='header'>
  <div class='header__wrapper'>
    <div class='header__logo'>
      <a href='/'><img src='https://placehold.jp/150x50.png' alt='ロゴ' /></a>
    </div>
    <div class='header__hamburger-toggle' id='header__hamburger-toggle'>
      <span></span>
      <span></span>
      <span></span>
    </div>
  </div>
</header>

<style>
  :root {
    --header-outer-height: 50px;
    --header-inner-height: 30px;
  }
  header {
    display: flex;
    align-items: center;
    top: calc((var(--header-outer-height) - var(--header-inner-height)) * -1);
    position: sticky;
    width: 100%;
    background-color: white;
    height: var(--header-outer-height);
    box-shadow: 0 2px 10px 0 rgba(0, 0, 0, 0.1);
  }

  header .header__wrapper {
    width: 90%;
    margin-left: auto;
    margin-right: auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: var(--header-inner-height);
    position: sticky;
    top: 0;
  }

  header .header__logo {
    height: 80%;
  }
  header .header__logo a,
  header .header__logo img {
    display: block;
    height: 100%;
  }

  header .header__wrapper .header__hamburger-toggle {
    position: relative;
    height: 12px;
    width: 16px;
    cursor: pointer;
  }

  header .header__wrapper .header__hamburger-toggle span {
    position: absolute;
    height: 1px;
    width: 100%;
    background-color: black;
    transition: all 0.3s;
  }

  header .header__wrapper .header__hamburger-toggle span:nth-child(1) {
    top: 0;
  }
  header .header__wrapper .header__hamburger-toggle span:nth-child(2) {
    top: calc(50% - 0.5px);
  }
  header .header__wrapper .header__hamburger-toggle span:nth-child(3) {
    bottom: 0;
  }

  header .header__wrapper .header__hamburger-toggle[open] span:nth-child(1) {
    transform: translateY(-50%) rotate(45deg);
    transform-origin: left;
  }
  header .header__wrapper .header__hamburger-toggle[open] span:nth-child(2) {
    opacity: 0;
  }
  header .header__wrapper .header__hamburger-toggle[open] span:nth-child(3) {
    transform: translateY(-50%) rotate(-45deg);
    transform-origin: left;
  }

  @media (min-width: 768px) {
    header {
      top: calc(((var(--header-outer-height) * 1.5) - (var(--header-inner-height)) * 1.5) * -1);
      height: calc(var(--header-outer-height) * 1.5);
    }

    header .header__wrapper {
      height: calc(var(--header-inner-height) * 1.5);
    }

    header .header__logo {
      height: 85%;
    }
    header .header__wrapper .header__hamburger-toggle {
      height: 20px;
      width: 26px;
    }
  }
</style>
